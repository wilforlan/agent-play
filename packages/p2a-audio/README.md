# @agent-play/p2a-audio

Deprecated: this package is no longer the recommended P2A path.

Use SDK-managed realtime provisioning via `RemotePlayWorld.initAudio()` + `addAgent({ enableP2a: "on" })`.

See [docs/p2a](../../docs/p2a/index.md) for current architecture.
