name: Publish npm packages

on:
  workflow_dispatch:
    inputs:
      publish_node_tools:
        type: boolean
        description: Publish @agent-play/node-tools
        default: true
      publish_intercom:
        type: boolean
        description: Publish @agent-play/intercom (after node-tools)
        default: true
      publish_sdk:
        type: boolean
        description: Publish @agent-play/sdk
        default: true
      publish_cli:
        type: boolean
        description: Publish @agent-play/cli
        default: true
      publish_play_ui:
        type: boolean
        description: Publish @agent-play/play-ui
        default: true
  push:
    branches:
      - main
    tags:
      - "v*"

jobs:
  changes:
    runs-on: ubuntu-latest
    outputs:
      node_tools: ${{ steps.pick.outputs.node_tools }}
      intercom: ${{ steps.pick.outputs.intercom }}
      sdk: ${{ steps.pick.outputs.sdk }}
      cli: ${{ steps.pick.outputs.cli }}
      play_ui: ${{ steps.pick.outputs.play_ui }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: dorny/paths-filter@v3
        id: filter
        if: github.event_name == 'push'
        with:
          filters: |
            node_tools:
              - 'packages/node-tools/**'
            intercom:
              - 'packages/intercom/**'
            sdk:
              - 'packages/sdk/**'
            cli:
              - 'packages/cli/**'
            play_ui:
              - 'packages/play-ui/**'
      - id: pick
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            echo "node_tools=${{ github.event.inputs.publish_node_tools }}" >> $GITHUB_OUTPUT
            echo "intercom=${{ github.event.inputs.publish_intercom }}" >> $GITHUB_OUTPUT
            echo "sdk=${{ github.event.inputs.publish_sdk }}" >> $GITHUB_OUTPUT
            echo "cli=${{ github.event.inputs.publish_cli }}" >> $GITHUB_OUTPUT
            echo "play_ui=${{ github.event.inputs.publish_play_ui }}" >> $GITHUB_OUTPUT
          else
            echo "node_tools=${{ steps.filter.outputs.node_tools }}" >> $GITHUB_OUTPUT
            echo "intercom=${{ steps.filter.outputs.intercom }}" >> $GITHUB_OUTPUT
            echo "sdk=${{ steps.filter.outputs.sdk }}" >> $GITHUB_OUTPUT
            echo "cli=${{ steps.filter.outputs.cli }}" >> $GITHUB_OUTPUT
            echo "play_ui=${{ steps.filter.outputs.play_ui }}" >> $GITHUB_OUTPUT
          fi

  publish:
    needs: changes
    if: |
      needs.changes.outputs.node_tools == 'true' ||
      needs.changes.outputs.intercom == 'true' ||
      needs.changes.outputs.sdk == 'true' ||
      needs.changes.outputs.cli == 'true' ||
      needs.changes.outputs.play_ui == 'true'
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          registry-url: "https://registry.npmjs.org"
      - name: Install dependencies
        run: npm install

      - name: Build @agent-play/node-tools (workspace dependency)
        if: |
          needs.changes.outputs.node_tools == 'true' ||
          needs.changes.outputs.intercom == 'true' ||
          needs.changes.outputs.sdk == 'true' ||
          needs.changes.outputs.cli == 'true' ||
          needs.changes.outputs.play_ui == 'true'
        run: npm run build -w @agent-play/node-tools

      - name: Build @agent-play/intercom
        if: |
          needs.changes.outputs.intercom == 'true' ||
          needs.changes.outputs.sdk == 'true' ||
          needs.changes.outputs.play_ui == 'true'
        run: npm run build -w @agent-play/intercom

      - name: Build and publish @agent-play/node-tools
        if: needs.changes.outputs.node_tools == 'true'
        run: |
          npm publish -w @agent-play/node-tools --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Publish @agent-play/intercom
        if: needs.changes.outputs.intercom == 'true'
        run: |
          npm publish -w @agent-play/intercom --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Publish @agent-play/sdk
        if: needs.changes.outputs.sdk == 'true'
        run: |
          npm run build -w @agent-play/sdk
          npm publish -w @agent-play/sdk --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Build and publish @agent-play/cli
        if: needs.changes.outputs.cli == 'true'
        run: |
          npm run build -w @agent-play/cli
          npm publish -w @agent-play/cli --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Build and publish @agent-play/play-ui
        if: needs.changes.outputs.play_ui == 'true'
        run: |
          npm run build -w @agent-play/play-ui
          npm publish -w @agent-play/play-ui --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
